# 스마트공장기능사 필기

스마트공장기능사 필기 문제은행 1,207문항으로 연습·종합시험을 푸는 도구입니다.

- 주소 : (아직 배포 전 — 10번 칸에서 `smart-factory-cbt` 로 배포합니다)
- 대상 : 스마트공장기능사 필기 준비 (스마트팩토리·메카트로닉스)
- 만든 날 : 2026-09-21

## 구성

| 파일 | 내용 |
|---|---|
| `index.html` | 범위 고르기 → 연습 8문항 → 종합시험 20문항 → 오답노트 → 제출 |
| `data/bank.js` | 문제은행 (자동 생성 · **커밋하지 않음**) |
| `build_bank.py` | 전사본 JSON 16개 → `data/bank.js` 굽기 |
| `rubric.json` | 결과수집이 세특 문장·성취수준을 만들 때 쓰는 평가 루브릭 |

## 문제은행 굽기

문항은 `index.html` 에 없습니다. 전사 작업방에서 가져와 굽습니다.

```
python build_bank.py
```

읽는 곳 : `../_작업/스마트공장 전사/_src/문항/*.json` (예상문제 13챕터 + CBT 3회)

| 칸 | 뜻 |
|---|---|
| `i` `c` `s` `p` | 아이디 · 챕터 · 절 · 책 쪽 |
| `q` `o` `a` `e` | 발문 · 보기 4개 · 정답(0부터) · 해설 |
| `d` | 중복 문항이면 먼저 나온 아이디 — **무작위 출제에서 뺀다** |
| `x` | 1이면 보기를 섞지 않는다 (해설이 "①, ③, ④는…" 처럼 보기 번호를 가리키는 문항) |

| 묶음 | 문항 |
|---|---|
| 예상문제 `SF_BANK` | 1,027 (중복 32 빼면 출제 대상 995) |
| CBT 모의고사 `SF_EXAMS` | 3회 × 60 = 180 (실전 화면은 9번 칸에서 만듭니다) |

## 두 모드 (규약 2)

| | 연습 | 종합시험 |
|---|---|---|
| 문항 수 | 8 | 20 |
| 제출 직후 | 정답·해설 바로 | 아무것도 안 보여 줌 |
| 진행 중 점수 | 실시간 | 감춤 |
| 끝난 뒤 | 점수 + 배너 + 오답노트 | 오답노트 → 제출 버튼 |

문제도 보기도 섞습니다. 주소에 `?fix=1` 을 붙이면 반 전체가 같은 문제·같은 보기 순서를 받습니다(`fixorder.js`).
풀다 창을 닫아도 **이어 풀기**로 같은 문제·같은 보기 순서에서 다시 시작합니다.

## 원자료 · 저작권

「2026 스마트공장기능사 필기」(건기원) 스캔본 512쪽을 옮긴 것입니다.
**시중 판매 교재라 평문 문항은 공개 저장소에 올리지 않습니다** — `data/bank.js` 는 `.gitignore` 에 있습니다.
배포본은 10번 칸에서 `bank.enc`(AES-GCM 암호 잠금, 공용 암호 `hong2281`)로 바꿔 답니다.

전사·검수 기록 : `../_작업/스마트공장 전사/` (`작업목록.md` · `검수보고.md`)

## 규약

제작 규약: https://github.com/hongyul67-cpu/links/blob/master/CONVENTIONS.md
